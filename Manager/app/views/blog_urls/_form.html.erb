<%= form_for(@blog_url) do |f| %>
  <% if @blog_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_url.errors.count, "error") %> prohibited this blog_url from being saved:</h2>

      <ul>
      <% @blog_url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :blog_url %><br />
    <%= f.text_field :blog_url %>
  </div>
  <div class="field">
    <%= f.label :User %><br />
    <%= f.text_field :User %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
