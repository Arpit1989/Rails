<% @blog.tags.build %>
<%= form_for(@blog) do |blog_form| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= blog_form.label :name %><br />
    <%= blog_form.text_field :name %>
  </div>
  <div class="field">
    <%= blog_form.label :title %><br />
    <%= blog_form.text_field :title %>
  </div>
  <div class="field">
    <%= blog_form.label :content %><br />
    <%= blog_form.text_area :content %>
  </div>
  <h2>Tags</h2>
  <%= render :partial => 'tags/form',
             :locals => {:form => blog_form} %>
  <div class="actions">
    <%= blog_form.submit %>
  </div>
<% end %>
